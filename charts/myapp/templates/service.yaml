apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}

spec:
  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http

  selector:
    app: {{ .Release.Name }}

  type: ClusterIP
